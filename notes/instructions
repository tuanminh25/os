## hlt
halt: stop cpu from executing - can be resumed by an interup

## jmp
jump to somewhere

## [var]
memory address of var

## lodsb, lodsw, lodsd
these instruction load byte/ word/ double
from ds:si into al/ax/eax
then increment SI by the number of bytes loaded

## jz
jump if zero flag is set

## pusha
save all register to stacks

## stc
set carry flag, some BIOS dont set it

## popa
return all register val

##test
actually an AND operation

## cli
disable the interuption